from setuptools import setup, find_packages

setup(
    name="program",
    version="1.0",
    description="A Python implementation of FuncA",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
)
